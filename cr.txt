api_classes.py

V    res = [i for a in res for i in a] - still unreadable, try to find another way to make it work

V    kwargs[value]=value + "="+ kwargs[value] - try to use f-string final_url=url+*?'+query_url - 513

supercli.py

V   datetime.datetime.date - better to use "from datetime import datetime" filter_by location - flag - change name filter_by_origin - flag - change name filter_by_season - flag - change name
    
    all filter function are duplicates, try to make it generic
    
most _frequent_character:
    
    for i, name - what is i?
    
    and if you need list without duplicates try to use set